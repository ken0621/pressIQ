var login = login()
      var ajaxdata = {};

      function login()
      {
        init();
      }
      function init()
      {
          $document.ready(function()
          {

          $document.ready();
          })
        }
      function document_ready()
        {
          event_prevent_reload();
          event_click_login();
        }
      function event_prevent_reload()
      {
         $('form.login').submit(function(e) {
          e.preventDefault();
          alert('123');
      }

      function event_click_login()
      {




      }
